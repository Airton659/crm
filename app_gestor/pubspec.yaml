name: grupo_solar_gestor
description: App mobile para gestão de leads - Grupo Solar Brasil
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Firebase
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0
  firebase_auth: ^5.0.0
  firebase_analytics: ^11.0.0

  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # Charts
  fl_chart: ^0.66.0

  # UI Components
  intl: ^0.20.2
  timeago: ^3.6.0

  # Dependency Injection
  get_it: ^7.6.4

  # Functional Programming
  dartz: ^0.10.1

  # Utilities
  cached_network_image: ^3.3.1
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/logo.png
